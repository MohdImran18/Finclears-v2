Schema::create('itr_appeals', function (Blueprint $table) {
            /*
        |--------------------------------------------------------------------------
        | Primary Key
        |--------------------------------------------------------------------------
        */

        $table->id();

        /*
        |--------------------------------------------------------------------------
        | Relationships
        |--------------------------------------------------------------------------
        */

        $table->foreignId('itr_return_id')
            ->nullable()
            ->constrained('itr_returns')
            ->nullOnDelete();

        $table->foreignId('itr_notice_id')
            ->nullable()
            ->constrained('itr_notices')
            ->nullOnDelete();

        $table->foreignId('user_id')
            ->constrained('users')
            ->cascadeOnDelete();

        $table->foreignId('company_id')
            ->nullable()
            ->constrained('companies')
            ->nullOnDelete();

        /*
        |--------------------------------------------------------------------------
        | Appeal Identification
        |--------------------------------------------------------------------------
        */

        $table->string('appeal_number', 100)
            ->unique();

        $table->string('appeal_reference_number', 100)
            ->nullable();

        $table->string('appeal_filing_number', 100)
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Appeal Classification
        |--------------------------------------------------------------------------
        */

        $table->enum('appeal_level', [
            'cit_a',
            'itat',
            'high_court',
            'supreme_court'
        ]);

        $table->string('appeal_authority', 255)
            ->nullable();

        $table->enum('appeal_type', [
            'assessment',
            'penalty',
            'refund',
            'tds',
            'rectification',
            'revision',
            'other'
        ]);

        $table->string('section', 50)
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Appeal Status
        |--------------------------------------------------------------------------
        */

        $table->enum('status', [
            'draft',
            'filed',
            'under_review',
            'admitted',
            'hearing_scheduled',
            'pending_order',
            'allowed',
            'partially_allowed',
            'dismissed',
            'withdrawn',
            'closed'
        ])->default('draft');

        /*
        |--------------------------------------------------------------------------
        | Appeal Timeline
        |--------------------------------------------------------------------------
        */

        $table->date('filing_date')
            ->nullable();

        $table->date('admission_date')
            ->nullable();

        $table->date('hearing_date')
            ->nullable();

        $table->date('next_hearing_date')
            ->nullable();

        $table->date('decision_date')
            ->nullable();

        $table->date('closed_date')
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Basic Details
        |--------------------------------------------------------------------------
        */

        $table->string('subject', 500)
            ->nullable();

        $table->longText('appeal_summary')
            ->nullable();

        $table->longText('department_observation')
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Flags
        |--------------------------------------------------------------------------
        */

        $table->boolean('is_active')
            ->default(true);

        $table->boolean('is_closed')
            ->default(false);

        $table->boolean('hearing_required')
            ->default(true);

        $table->text('remarks')
            ->nullable();
                    /*
        |--------------------------------------------------------------------------
        | Financial Dispute
        |--------------------------------------------------------------------------
        */

        $table->decimal('tax_amount', 15, 2)
            ->default(0);

        $table->decimal('interest_amount', 15, 2)
            ->default(0);

        $table->decimal('penalty_amount', 15, 2)
            ->default(0);

        $table->decimal('refund_amount', 15, 2)
            ->default(0);

        $table->decimal('disputed_amount', 15, 2)
            ->default(0);

        /*
        |--------------------------------------------------------------------------
        | Appeal Grounds
        |--------------------------------------------------------------------------
        */

        $table->longText('grounds_of_appeal')
            ->nullable();

        $table->longText('statement_of_facts')
            ->nullable();

        $table->longText('legal_arguments')
            ->nullable();

        $table->longText('relief_sought')
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Government Integration
        |--------------------------------------------------------------------------
        */

        $table->boolean('government_synced')
            ->default(false);

        $table->timestamp('government_synced_at')
            ->nullable();

        $table->string('api_provider', 100)
            ->nullable();

        $table->integer('api_status_code')
            ->nullable();

        $table->integer('api_response_time')
            ->nullable()
            ->comment('Response time in milliseconds');

        $table->json('api_request')
            ->nullable();

        $table->json('api_response')
            ->nullable();

        $table->string('api_reference_number', 100)
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | AI Case Analysis
        |--------------------------------------------------------------------------
        */

        $table->boolean('ai_analyzed')
            ->default(false);

        $table->timestamp('ai_analyzed_at')
            ->nullable();

        $table->decimal('success_probability', 5, 2)
            ->nullable();

        $table->decimal('ai_confidence_score', 5, 2)
            ->nullable();

        $table->enum('risk_level', [
            'low',
            'medium',
            'high',
            'critical'
        ])->default('medium');

        $table->json('recommended_actions')
            ->nullable();

        $table->json('required_documents')
            ->nullable();

        $table->json('similar_case_references')
            ->nullable();

        $table->longText('ai_summary')
            ->nullable();

        $table->json('validation_errors')
            ->nullable();

        $table->json('warnings')
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Appeal Documents
        |--------------------------------------------------------------------------
        */

        $table->string('appeal_memo_file_name', 255)
            ->nullable();

        $table->string('appeal_memo_file_path')
            ->nullable();

        $table->string('order_file_name', 255)
            ->nullable();

        $table->string('order_file_path')
            ->nullable();

        $table->json('supporting_documents')
            ->nullable();

        /*
        |--------------------------------------------------------------------------
        | Metadata
        |--------------------------------------------------------------------------
        */

        $table->string('ip_address', 45)
            ->nullable();

        $table->string('device_type', 100)
            ->nullable();

        $table->string('browser', 150)
            ->nullable();

        $table->string('operating_system', 150)
            ->nullable();

        $table->uuid('appeal_session')
            ->nullable();
                    /*
        |--------------------------------------------------------------------------
        | Enterprise Metadata
        |--------------------------------------------------------------------------
        */

        $table->uuid('uuid')->unique();

        $table->unsignedInteger('version')
            ->default(1);

        /*
        |--------------------------------------------------------------------------
        | Audit Trail
        |--------------------------------------------------------------------------
        */

        $table->foreignId('created_by')
            ->nullable()
            ->constrained('users')
            ->nullOnDelete();

        $table->foreignId('updated_by')
            ->nullable()
            ->constrained('users')
            ->nullOnDelete();

        /*
        |--------------------------------------------------------------------------
        | Timestamps
        |--------------------------------------------------------------------------
        */

        $table->timestamps();

        $table->softDeletes();

        /*
        |--------------------------------------------------------------------------
        | Performance Indexes
        |--------------------------------------------------------------------------
        */

        $table->index('itr_return_id');
        $table->index('itr_notice_id');
        $table->index('user_id');
        $table->index('company_id');

        $table->index('appeal_number');
        $table->index('appeal_reference_number');
        $table->index('appeal_filing_number');

        $table->index('appeal_level');
        $table->index('appeal_type');
        $table->index('section');

        $table->index('status');
        $table->index('is_active');
        $table->index('is_closed');

        $table->index('government_synced');
        $table->index('ai_analyzed');

        $table->index('risk_level');

        $table->index('filing_date');
        $table->index('admission_date');
        $table->index('hearing_date');
        $table->index('next_hearing_date');
        $table->index('decision_date');
        $table->index('closed_date');

        $table->index('api_reference_number');

        $table->index('appeal_session');

        $table->index('uuid');

        $table->index('created_at');

        /*
        |--------------------------------------------------------------------------
        | Composite Indexes
        |--------------------------------------------------------------------------
        */

        $table->index([
            'user_id',
            'status'
        ]);

        $table->index([
            'company_id',
            'status'
        ]);

        $table->index([
            'itr_return_id',
            'status'
        ]);

        $table->index([
            'itr_notice_id',
            'status'
        ]);

        $table->index([
            'appeal_level',
            'status'
        ]);

        $table->index([
            'appeal_type',
            'status'
        ]);

        $table->index([
            'risk_level',
            'status'
        ]);

        $table->index([
            'government_synced',
            'status'
        ]);

        $table->index([
            'ai_analyzed',
            'status'
        ]);

        $table->index([
            'status',
            'next_hearing_date'
        ]);

        $table->index([
            'status',
            'decision_date'
        ]);

        $table->index([
            'appeal_level',
            'appeal_type'
        ]);

        $table->index([
            'appeal_number',
            'appeal_level'
        ]);
                /*
        |--------------------------------------------------------------------------
        | Business Rule Constraints
        |--------------------------------------------------------------------------
        */

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_level
            CHECK (
                appeal_level IN (
                    'cit_a',
                    'itat',
                    'high_court',
                    'supreme_court'
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_type
            CHECK (
                appeal_type IN (
                    'assessment',
                    'penalty',
                    'refund',
                    'tds',
                    'rectification',
                    'revision',
                    'other'
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_status
            CHECK (
                status IN (
                    'draft',
                    'filed',
                    'under_review',
                    'admitted',
                    'hearing_scheduled',
                    'pending_order',
                    'allowed',
                    'partially_allowed',
                    'dismissed',
                    'withdrawn',
                    'closed'
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_risk
            CHECK (
                risk_level IN (
                    'low',
                    'medium',
                    'high',
                    'critical'
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_amounts
            CHECK (
                tax_amount >= 0
                AND interest_amount >= 0
                AND penalty_amount >= 0
                AND refund_amount >= 0
                AND disputed_amount >= 0
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_success_probability
            CHECK (
                success_probability IS NULL
                OR (
                    success_probability >= 0
                    AND success_probability <= 100
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_ai_confidence
            CHECK (
                ai_confidence_score IS NULL
                OR (
                    ai_confidence_score >= 0
                    AND ai_confidence_score <= 100
                )
            )
        ");

        DB::statement("
            ALTER TABLE itr_appeals
            ADD CONSTRAINT chk_itr_appeals_version
            CHECK (
                version >= 1
            )
        ");
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::disableForeignKeyConstraints();

        Schema::dropIfExists('itr_appeals');

        Schema::enableForeignKeyConstraints();
    }
};